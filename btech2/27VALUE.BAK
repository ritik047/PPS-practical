#include<stdio.h>
#include<conio.h>

void swap(int a,int b)
{
	int temp;
	temp=a;
	a=b;
	b=temp;
	printf("\nAfter swaping values in swap function:\na=%d\nb=%d",a,b);
}
void main()
{
	int a,b;
	clrscr();
	printf("Enter the value of a:");
	scanf("%d",&a);
	printf("\nEnter the value of b:");
	scanf("%d",&b);
	swap(a,b);
	printf("\nIn the main function after swaping the values:\na=%d\nb=%d",a,b);

	getch();
}